apiVersion: v1
kind: Service
metadata:
  name: ci-docker-project-service
spec:
  selector:
    app: ci-docker-project
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
